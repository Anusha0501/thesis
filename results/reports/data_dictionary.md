# Data Dictionary

Status: pending execution against downloaded Zenodo CSV files.

The data dictionary generator profiles every column in every loaded CSV and writes actual dtypes, null counts, unique counts, numeric ranges, and inferred meanings. This placeholder intentionally contains no invented column statistics.

Run:

```bash
python src/preprocessing/audit_reports.py
```
